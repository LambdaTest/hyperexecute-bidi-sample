---
version: 0.1
autosplit: true

concurrency: 2
runson: mac

runtime:
  language: node
  version: 18

pre:
  - npm install

testDiscovery:
  mode: static
  type: raw
  command: echo "test/specs/BrowsingContext.spec.js"

testRunnerCommand: npx wdio wdio.lambdatest.conf.js --spec=test/specs/BrowsingContext.spec.js

jobLabel: [HYP, BiDi, macOS]